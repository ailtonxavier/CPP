#include <iostream>

using namespace std;

int main(){
  int a, b, adicao, subtracao, multiplicacao, divisao;
  cin >> a;
  cin >> b;
  adicao = a + b;
  subtracao = a - b;
  multiplicacao = a * b;
  divisao = a / b;
}